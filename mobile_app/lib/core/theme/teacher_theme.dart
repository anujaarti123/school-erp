import 'package:flutter/material.dart';

/// Premium Teacher theme — amber/gold palette (wisdom, professional)
class TeacherColors {
  static const Color primary = Color(0xFFB45309);       // Deep amber
  static const Color primaryLight = Color(0xFFD97706); // Amber
  static const Color accent = Color(0xFF92400E);        // Dark amber
  static const Color background = Color(0xFFF8FAFC);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color textPrimary = Color(0xFF1E293B);
  static const Color textSecondary = Color(0xFF64748B);
  static const Color success = Color(0xFF059669);
  static const Color error = Color(0xFFDC2626);
}
